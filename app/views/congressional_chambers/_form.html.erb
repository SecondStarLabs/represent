<%= form_with(model: congressional_chamber, local: true) do |form| %>
  <% if congressional_chamber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(congressional_chamber.errors.count, "error") %> prohibited this congressional_chamber from being saved:</h2>

      <ul>
      <% congressional_chamber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if congressional_chamber.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', congressional_chamber, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if congressional_chamber.persisted? %>
      <%= link_to "Cancel", congressional_chamber, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", congressional_chambers_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
